<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="/css/shoppingCart.css">
    <link rel="stylesheet" href="/css/newShoppingCartItem.css">
    <link rel="stylesheet" href="/css/general.css">
    <link rel="stylesheet" href="/css/navbar.css">
    <title>Zum Einkaufswagen hinzugefügt</title>
</head>
<body>
<h1>Neues Produkt im Einkaufswagen</h1>
<%- include('navbar') %>
<h3>Zum Einkaufswagen hinzugefügt:</h3>
<div class="added-item">
    <div class="image-container">
        <img class="image--small"
             src="<%= productImage %>">
    </div>
    <div class="product-name">
        <%= productName %>
    </div>
    <div class="product-price">
        <%= devicePrice %> €
    </div>
</div>
<wertgarantie-selection-pop-up id="selection"
                               data-shop-product-name="<%= productName %>"
                               data-bifrost-uri="<%= bifrostUriForFEComponents %>">
</wertgarantie-selection-pop-up>

<script type="module" src="<%= popupComponentUri %>" crossorigin="anonymous"></script>
<script>
    window.onload = function () {
        var selection = document.getElementById('selection');
        selection.devicePrice = "<%= devicePrice %>";
        selection.deviceClass = "<%= deviceClass %>";
        selection.clientId = "<%= clientId %>";
        selection.displayComponent();
    }
</script>
</body>
</html>