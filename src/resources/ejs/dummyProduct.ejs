<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dummy-Produkt</title>
    <link rel="stylesheet" href="/css/dummyProduct.css">
    <link rel="stylesheet" href="/css/general.css">
    <link rel="stylesheet" href="/css/navbar.css">
</head>
<body>
<h1><%= product.name %></h1>
<%- include('navbar') %>
<div class="product-selection">
    <div class="product-selection__image">
        <img src="<%= product.image %>" alt="nokia phone">
    </div>
    <div class="product-selection__info">
        <h3>Produktinformation</h3>
        <div class="info__description">
            <p><%= product.name %></p>
            <p>Details:</p>
            <ul>
                <% product.attributes.forEach(attribute => { %>
                    <li><%= attribute %></li>
                <% }); %>
            </ul>
        </div>
        <div class="variant-selection">
            <select name="variant" id="variant" onchange="swapVariants()">
                <% product.variants.forEach(variant => { %>
                    <option value="<%= variant.id%>" <%= variant.id === selectedVariant.id ? 'selected="selected"' : ''%>><%= variant.name %></option>
                <% }); %>
            </select>
        </div>
        <div class="info__description">
            <h4>Preis:</h4>
            <p><%= selectedVariant.devicePrice / 100 %> €</p>
        </div>
        <div class="order-panel">
            <form>
                <input type="hidden" value="<%= product.name + ' ' + product %>" name="productName">
                <input type="hidden" value="<%= product.id %>" name="productId">
                <input type="hidden" value="<%= selectedVariant.id %>" name="variantId">
                <button class="submit-button" formaction="/addToShoppingCart" formmethod="POST">In den Warenkorb
                </button>
            </form>
        </div>
        <div>
            <p>Erhalten Sie Versicherungsangebote der Wertgarantie zu diesem Produkt. Einfach in den Warenkorb
                legen.</p>
            Wertgarantie:
            <wertgarantie-rating data-bifrost-uri="<%= bifrostUriForFEComponents %>"></wertgarantie-rating>
        </div>
    </div>
</div>
<script>
    function swapVariants() {
        top.location.href = top.location.href.split("?variantId")[0] + "?variantId=" + document.getElementById('variant').value;
    }
</script>
<script type="module" src="<%= ratingComponentUri %>" crossorigin="anonymous"></script>
</body>
</html>